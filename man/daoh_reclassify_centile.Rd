% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compare.R
\name{daoh_reclassify_centile}
\alias{daoh_reclassify_centile}
\title{Centile-boundary reclassification across two DAOH methods}
\usage{
daoh_reclassify_centile(
  res_a,
  res_b,
  boundaries = c(0.05, 0.1, 0.9, 0.95),
  use_pc = TRUE
)
}
\arguments{
\item{res_a, res_b}{data.frames (output of \code{\link[=calc_daoh]{calc_daoh()}}). Must have columns
\code{patientID}, \code{indexDate}, and \code{daohPC} (or \code{daoh} if \code{use_pc = FALSE}).}

\item{boundaries}{Numeric vector of centile probabilities at which to
evaluate reclassification. Values below 0.5 define lower ("poor outcome")
boundaries; values at or above 0.5 define upper ("excellent outcome")
boundaries. Default \code{c(0.05, 0.10, 0.90, 0.95)}.}

\item{use_pc}{Logical. Use \code{daohPC} (default \code{TRUE}) or \code{daoh}.}
}
\value{
A data.frame with one row per boundary and columns:
\describe{
\item{\code{boundary}}{Human-readable label, e.g. \code{"Bottom 5\%"} or
\code{"Top 10\%"}.}
\item{\code{centile}}{The probability supplied in \code{boundaries}.}
\item{\code{threshold_a}}{Empirical centile value for algorithm A.}
\item{\code{threshold_b}}{Empirical centile value for algorithm B.}
\item{\code{n_patients}}{Number of matched patient-index pairs evaluated.}
\item{\code{n_reclassified}}{Number of patients classified differently
across the boundary.}
\item{\code{pct_reclassified}}{Percentage classified differently.}
}
}
\description{
For each specified centile boundary, classifies patients as inside or
outside that boundary under each algorithm — using each algorithm's own
empirical threshold — then reports the proportion classified differently.
This quantifies the clinical impact of algorithm choice at cut-points such
as "bottom 10\% poor outcome", which are common in adaptive trial enrichment
and secondary outcome definitions.
}
\details{
Unlike \code{\link[=daoh_reclassify]{daoh_reclassify()}}, which uses a pooled distribution to set group
boundaries, this function applies each algorithm's threshold independently.
That reflects the realistic scenario in which a researcher picks a single
algorithm, defines a centile-based threshold from their own study population,
and applies it — so the threshold itself changes with the algorithm.
}
\examples{
# See vignette("getting_started", package = "daoh")
}
\seealso{
\code{\link[=daoh_reclassify]{daoh_reclassify()}} for group-based (quartile) reclassification.
}
