% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_intervals.R
\name{merge_intervals}
\alias{merge_intervals}
\title{Merge overlapping or near-adjacent time intervals}
\usage{
merge_intervals(starts, ends, gap = 0.5)
}
\arguments{
\item{starts}{Numeric or POSIXct vector of interval start times.}

\item{ends}{Numeric or POSIXct vector of interval end times (must be >=
the corresponding start).}

\item{gap}{Numeric. Intervals with a gap smaller than this value are
merged. Units must match \code{starts}/\code{ends}. Default 0.5 (= 12 hours if
times are in days).}
}
\value{
A data.frame with columns \code{start} and \code{end} representing the
merged intervals, sorted by start time.
}
\description{
Given a set of intervals [start, end], merges those that overlap or are
separated by less than \code{gap} time units. This is used to consolidate
hospital admissions that are separated by short gaps (e.g., < 12 hours),
treating them as a single continuous episode.
}
\examples{
# Two admissions 10 hours apart -> merged into one
merge_intervals(
  starts = c(0, 1.5),
  ends   = c(1.0, 2.5),
  gap    = 0.5
)

# Two admissions 2 days apart -> kept separate
merge_intervals(
  starts = c(0, 3),
  ends   = c(1, 4),
  gap    = 0.5
)

}
